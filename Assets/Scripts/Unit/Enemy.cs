using Turn_Based_Combat.Character;
using UnityEngine;

namespace Unit
{
    public class Enemy : BaseUnit
    {
        public void DoSomething(BaseUnit player)
        {
            // var enemyStatus = (EnemyStatus)status;
            // enemyStatus.behaviour[0].DoAction(player);

            Debug.Log("adc");
        }
    }
}